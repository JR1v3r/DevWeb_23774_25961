@model IEnumerable<DevWeb_23774_25961.Models.Livros>

@{
    ViewData["Title"] = "Resultados de Pesquisa";
}

<div class="container mt-4">
    <h2 class="mb-4">@ViewData["Title"]</h2>

    <!-- Formulário de pesquisa 
    <form asp-controller="Search" asp-action="Index" method="get" class="mb-4 w-100">
        <div class="input-group">
            <input class="form-control rounded-pill ps-4 pe-5" 
                   type="search" 
                   name="query" 
                   value="@Context.Request.Query["query"]"
                   placeholder="Pesquisar livros, autores, ISBNs" 
                   aria-label="Pesquisa">
            <button class="btn position-absolute end-0 top-50 translate-middle-y me-2 text-purple" 
                    type="submit" 
                    style="background: none; border: none;">
                <i class="bi bi-search"></i>
            </button>
        </div>
    </form>-->

    @if (!Model.Any())
    {
        <div class="alert alert-info">
            Nenhum livro encontrado para a sua pesquisa.
        </div>
    }
    else
    {
        <div class="row">
            @foreach (var livro in Model)
            {
                <div class="col-md-3 mb-4">
                    <div class="card h-100 shadow-sm" style="cursor: pointer;" onclick="window.location.href='@Url.Action("Details", "Livros", new { id = livro.Id })'">
                        <img src="@livro.Capa" class="card-img-top" style="height: 250px; object-fit: cover;" alt="Capa do Livro">
                        <div class="card-body">
                            <h5 class="card-title">@livro.Titulo</h5>
                            <p class="card-text text-muted">Autor: @livro.Autor</p>
                            <p class="card-text"><small>ISBN: @livro.ISBN</small></p>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>