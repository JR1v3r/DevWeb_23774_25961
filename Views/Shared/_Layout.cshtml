<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>@ViewData["Title"] - BookTradesProject</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true"/>
    <link rel="stylesheet" href="~/css/herosection.css"/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="~/BookTradesProject.styles.css" asp-append-version="true"/>
</head>
<body>
<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm py-2">
        <div class="container-fluid flex-column">

            <!-- Top Row: Logo + Search + Sign-in -->
            <div class="d-flex w-100 justify-content-between align-items-center mb-2 px-1">
                <!-- Logo + Brand -->
                <a class="navbar-brand d-flex align-items-center" asp-controller="Home" asp-action="Index">
                    <div class="bg-dark text-white d-flex justify-content-center align-items-center rounded-1" style="width: 40px; height: 40px; font-size: 0.6rem; font-weight: bold; transform: rotate(-45deg);">
                        LogoHere
                    </div>
                    <span class="ms-2 fw-bold fs-5">Book 'n' Swap™</span>
                </a>

                <!-- Center Search Bar -->
                <form class="w-50 position-relative mx-2">
                    <input class="form-control rounded-pill ps-4 pe-5" type="search" placeholder="Search books, authors, ISBNs" aria-label="Search">
                    <button class="btn position-absolute end-0 top-50 translate-middle-y me-2 text-purple" type="submit" style="background: none; border: none;">
                        <i class="bi bi-search"></i>
                    </button>
                </form>

                <!-- Sign-in + Bag -->
                <div class="d-flex align-items-center">
                    <partial name="_LoginPartial" />
                </div>
            </div>

            <!-- Bottom Row: Links -->
            <div class="w-100 d-flex justify-content-center">
                <ul class="nav">
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-controller="Collection" asp-action="Index">My collection</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-controller="Trade" asp-action="Notifications">Trade and Notification Center</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-controller="Account" asp-action="Manage">Manage Account</a>
                    </li>
                </ul>
            </div>

        </div>
    </nav>
</header>


<div class="container-fluid" id="main-container">
    <main role="main" class="pb-3">
        @RenderBody()
    </main>
</div>

<footer class="border-top footer text-muted">
    <div class="container">
        &copy; 2025 - BookTradesProject - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
    </div>
</footer>
<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/js/site.js" asp-append-version="true"></script>
@await RenderSectionAsync("Scripts", required: false)
</body>
</html>