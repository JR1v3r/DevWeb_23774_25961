@model IEnumerable<DevWeb_23774_25961.Models.Trades>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InventorySlot)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PreviousOwner)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CurrentOwner)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InventorySlot.Users_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PreviousOwner)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentOwner)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
